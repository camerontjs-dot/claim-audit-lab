name: Research - CAL semantic family unseen pressure RC1

on:
  push:
    branches: [research/cal-semantic-family-unseen-pressure-rc1-20260917]
  pull_request:
    paths:
      - "research/semantic_family_unseen_pressure_rc1/**"
      - "tests/research/test_semantic_family_unseen_pressure_rc1_apparatus.py"
      - ".github/workflows/cal-semantic-family-unseen-pressure-rc1.yml"

permissions:
  contents: read

jobs:
  pressure:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --all-extras
      - run: uv run python -m spacy download en_core_web_sm
      - name: Validate frozen unseen-pressure apparatus
        run: uv run python -m pytest -q tests/research/test_semantic_family_unseen_pressure_rc1_apparatus.py
      - name: Reconstruct exact qualified subjects
        run: |
          uv run python research/semantic_family_unseen_pressure_rc1/prepare_subjects.py \
            research/semantic_family_unseen_pressure_rc1/subjects.json \
            /tmp/cal-unseen-pressure
      - name: Run unseen cross-family pressure
        run: |
          uv run python research/semantic_family_unseen_pressure_rc1/run_pressure.py \
            research/semantic_family_unseen_pressure_rc1/subjects.json \
            research/semantic_family_unseen_pressure_rc1/cases.json \
            /tmp/cal-unseen-pressure \
            /tmp/cal-unseen-pressure-report.json
      - name: Upload unseen-pressure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cal-semantic-family-unseen-pressure-rc1
          path: /tmp/cal-unseen-pressure-report.json
      - name: Static checks
        if: always()
        run: uv run python -m ruff check research/semantic_family_unseen_pressure_rc1 tests/research/test_semantic_family_unseen_pressure_rc1_apparatus.py
