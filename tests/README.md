# Test plan

First tests should cover model validation, malformed evidence bundles, conservative claim extraction, numeric evidence matching, overstrong wording, report rendering, and CLI malformed-input behavior.

Assert behavior rather than private implementation details.

