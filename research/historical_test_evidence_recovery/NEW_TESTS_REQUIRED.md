---
title: "Smallest genuinely new tests still required"
privacy: "private-local"
---

# Smallest genuinely new tests still required

Prefer reuse of trustworthy historical evidence over ceremonial reruns. Prefer fresh execution when the current scientific claim actually requires it.

## Not required

- Rerun PILOT-001 to re-prove v0.2 falsification.
- Rerun construction-gold on v1.13.0 to re-prove 26/33.
- Rerun the 25 e2e shadow to re-prove PR #36 scientific-object equality (unless the sentinel check in the larger study requires it, which it does).
- NLI threshold sweep.
- Model bake-off before a typed numeric semantic target exists.
- Copying 39M SLG generated trees into Git.

## Required for the already-preregistered larger shadow (not executed here)

1. Generate **input-only** 33-case construction-gold manifest from `scripts/build_construction_gold.py` at blob `2c677ee2…`.
2. Assign phenomenon strata **before** successor/legacy comparison.
3. Hash the manifest.
4. Rerun the frozen 25-case sentinel; require normalized predecessor scientific object.
5. Then execute Cohort A through the exact #36/#37 real shadow apparatus.
6. Seal gold/FINDINGS from the execution aperture.

That is new execution on old inputs, not a new corpus design.

## Smallest new tests beyond that

| Test | Why historical work is not enough | Bound |
|---|---|---|
| Typed numeric/relational operator receipt on frozen e2e-08 | Historical comparator proves C6a unreachability and range collapse on **legacy** traces; it does not instrument the parallel artifact | 1 frozen case + 1–2 construction bound cases (CG-05, CG-12) |
| First-class `inapplicable` vs `unknown` on the A4-null e2e-08 probe | PR #37 NEXT; conceptual only today | research-artifact field only; no `src/` production change |
| Independent-consumer read of one parallel artifact | not implemented | 1–3 cases (e2e-08, e2e-15, CG-08b) |
| Cohort B SLG input-only freeze | constructor exists; successor freeze does not | 12 worlds, not the scaled tree |

## Explicitly out of scope unless separately authorized

- Landing a numeric comparator into production rules
- Changing 0.70/0.70
- Replacing DeBERTa
- Repairing e2e-08 disagreement so explicit path matches legacy `contradicted`
- Using construction-gold or PILOT-001 labels to choose operators
