---
title: "NEXT after local pipeline reconciliation"
privacy: "public-research-record"
---

# NEXT

## Recommended next step

**Local v2 contains genuinely important architecture nominations that require separate evaluation. Do not merge `feat/v2-epistemic-pipeline` into `main`.**

This reconciliation is complete as a **lineage and classification** record. Additional bounded recovery of dirty overlay *source* is not required on GitHub: hashes bind the files; the feature branch already holds the committed slice.

## Do next, separately

1. Keep Draft PRs #39–#41 as the live explicit-machinery research stack.
2. If a typed numeric/bound operator RC is opened, consider `interval_algebra.py` from `b903537` (plus dirty delta) as a **nomination**, with Cohort A monotonicity falsifiers from #41. That is a research PR, not this one.
3. Optionally extract the coverage-complement “must be declared” rule as a one-page invariant for the source-boundary experiment named in #39 NEXT.
4. Leave the dirty unpublished overlay dirty until an operator chooses to commit it to the feature branch or discard it. Do not clean it as part of `main`.

## Do not do from this record

- Merge v2 to `main`
- Point default CLI at `run_v2_audit`
- Treat 26/33 or v2 bench scripts as validation
- Rewrite public history for path leaks (pre-existing; follow-up hygiene)
- Execute SLG Cohort B to paper over operator gaps
