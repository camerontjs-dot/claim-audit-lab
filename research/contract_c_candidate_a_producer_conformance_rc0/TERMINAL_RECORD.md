# Contract C Candidate A producer conformance RC0 terminal record

## Terminal disposition

**`FALSIFIED`**

Exact Candidate A RC1 is not supported as the complete current CAL V1 producer surface.

The falsification is narrow. The minimal-sufficient-basis-group causal architecture survived the preregistered producer-conformance pressure. The decisive failure is that current CAL V1 legitimately emits `NOT_CHECKABLE / UNSUPPORTED_SEMANTIC_FAMILY`, while Candidate A RC1's closed public terminal-reason vocabulary cannot preserve `UNSUPPORTED_SEMANTIC_FAMILY` without deletion or relabelling.

This is a Draft Research disposition. It does not authorize a production CAL mutation, Contract C promotion, version assignment, release, tag, Decision Engine change, Contract E / Authorization change, or execution.

## Exact frozen authorities

CAL producer authority:

- exact CAL RC1: `a902621e8baea3063dddd7f92ba975aade305464`
- engine blob: `636734fd1341d2ae721ae9697c7ac7652b89eecc`
- relation blob: `e9e9401c92ee11d25a50dbc36e80aeb4c4cd220d`
- frozen predecessor projection blob: `9bc152275759304be03b84014c56bd434549a64a`

Apparatus Candidate A authority:

- exact RC1 freeze: `ba5f55b0fb6ca54e0461a688813b66fd81bf8c4c`
- contract blob: `0ab62b7ceea1d56d6bc3b7cf8528769dd440ab7c`
- schema blob: `0de83032e6d989225cf014d9a70035f634a5e003`
- validator blob: `03dbb5b774af523d8e6235bca86b18af5ddad5b0`
- frozen Phase 1 corpus blob: `0ff4f66bca00924755d42ed7f944c4dae8d10f66`
- frozen Phase 2 oracle blob: `9f00cd332ad366b878f60d13440af639360928de`

Frozen CAL experiment apparatus:

- preregistration blob: `3d438270cff832545dd2809fc5f53bd87f93e467`
- compose-only materializer blob: `08a4968a7999d05dfd65084c062adb552e127265`
- evaluator blob: `a8f9d9888abcda13ade50e46e8774ae642a955c1`
- workflow launch head: `abcb337dc5e58e2594775a298efcc050ef033e6f`

## Decisive execution

- workflow run: `34799018227`
- job: `103837717822`
- GitHub Actions conclusion: `success`
- artifact: `10330722658`
- artifact digest: `sha256:595297587f9b1d193b6c21e2b3578bc114c75bea6ee3c157663fe56815a962ba`
- extracted `EVALUATION.json` SHA-256: `63bc3d46b8762d51c9c07ca71572d68175588e216f6b24e93660cc43db9f88eb`
- frozen CAL RC1 control replay: PASS
- static compose-only materializer check: PASS
- exact Candidate A / Phase 1 / Phase 2 / resolver identity checks: PASS
- apparatus failures: none

A green workflow here means the research apparatus executed successfully. The scientific disposition remains `FALSIFIED` as preregistered.

## What passed

### Causal materialization from existing CAL authority

The materializer consumed only the exact already-produced `AuditResult` / `PassageTrace` state and unchanged `compose()` for subset replay. It did not call measurement, source completion, semantic authority, relation derivation, the old Contract C projector, or a scalar/learned decision procedure.

Observed interpretation:

`basis_groups_derived_from_existing_relations_plus_compose_only = true`

Therefore, within this tested CAL V1 fragment, the richer causal basis is supported as deterministic materialization of existing CAL-owned relation/composition state rather than a new exporter epistemic judgment.

### Frozen Phase 2 basis semantics

All exercised current-CAL / frozen-oracle cases matched exactly:

- single support: `{{S1}}`;
- single refutation: `{{R1}}`;
- independent supports: `{{S1},{S2}}`;
- independent refutations: `{{R1},{R2}}`;
- support + neutral residual: `{{S1}}`, with `N1` retained as `non_polarized / residual`;
- minimal mixed: `{{S1,R1}}`;
- CAL #106 alternative-joint mixed: `{{S1,R1},{S2,R1}}`;
- symmetric alternative-joint mixed: `{{S1,R1},{S1,R2}}`;
- four-way mixed generalization: `{{S1,R1},{S1,R2},{S2,R1},{S2,R2}}`;
- no-deciding: empty causal family with retained residual non-polarized evidence.

Candidate A structural validation, exact Contract-B reference validation, immutable policy resolution and deterministic repeat also passed.

### Cross-family pressure

Direct event-order support and refutation materialized the same one-member basis semantics without strict-comparison-specific causal logic.

The direct event-order reporting/scope refusal remained:

- completed;
- `not_checkable`;
- `no_deciding_relation`;
- empty basis family;
- exact evidence retained as `non_polarized / residual`.

### Weak flat control

The deliberately weak old-style flat strategy was killed exactly where required.

For CAL #106:

- weak flat result: `{{S1,S2,R1}}`;
- required/observed result: `{{S1,R1},{S2,R1}}`.

For the symmetric counterpart:

- weak flat result: `{{S1,R1,R2}}`;
- required/observed result: `{{S1,R1},{S1,R2}}`.

Thus the apparatus discriminated the target materializer from the already-falsified causal flattening strategy.

## Decisive falsifier

Current frozen CAL V1 deliberately fails closed for unsupported semantic families.

Exact exercised result:

- conclusion: `not_checkable`;
- failure/public reason: `UNSUPPORTED_SEMANTIC_FAMILY`.

This is a legitimate CAL-owned terminal distinction in the frozen V1 convergence scope, not an execution failure and not `no_deciding_relation`.

Observed Candidate A RC1 boundary:

- the compose-only materializer refuses the reason as unrepresentable;
- exact Candidate A RC1 rejects `UNSUPPORTED_SEMANTIC_FAMILY` under its closed terminal-reason vocabulary;
- therefore preserving the current CAL result would require dropping or relabelling semantic state.

Preregistered check:

`candidate_a_can_preserve_unsupported_family_reason = false`

This satisfies falsifier F7.

## Interpretation

The result separates two questions that were previously entangled.

1. **Causal representation:** supported within the tested producer fragment. Minimal sufficient basis groups faithfully recover the demonstrated CAL causal multiplicity, including the #106 counterexample, from existing relation/composition authority.
2. **Exact Candidate A RC1 surface completeness:** falsified. Its terminal-reason vocabulary omitted one legitimate current V1 reason.

Accordingly:

- `basis_group_architecture_falsified_by_unsupported_reason_gap = false`;
- `candidate_a_rc1_complete_current_v1_producer_surface = false`;
- `old_flat_causal_strategy_discriminated = true`;
- no official Contract C version is assigned;
- production promotion remains unauthorized.

The smallest evidence-justified successor question is now a terminal-reason vocabulary repair, not a reopening of the causal architecture. The frozen RC1 candidate and this falsification must remain unchanged.