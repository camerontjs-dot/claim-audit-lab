# Construction Cohort A Parallel Epistemic Shadow — Execution Preregistration

## Classification

Research Infrastructure / epistemic-machinery larger-shadow execution.

This study executes the already-preregistered Cohort A concept from PR #37 using the recovered historical-test inventory in PR #38 only to avoid redundant legacy retesting. It does not use historical verdicts as adaptation gold.

## Frozen authority

- production main: `53f0885b111676794d1bd20e10b91aa58b07e9d4`
- predecessor #36 head: `b487d1dce4cc1a076e3705b0a7ef457e7d438814`
- predecessor #37 head consumed by this branch: `64d05b731a4f5b4c0a829a8f03b252fa0103d7fe`
- canonical construction builder blob: `2c677ee29fd121cf1c76b1476664474aa09dc982`
- frozen 25-case sentinel normalized scientific-object SHA256:
  `db6ac4dac26045c0b489cf28a704376b659522979759e33f704706c61472c51d`

## Execution aperture

The Cohort A runner may consume only an input-only manifest containing:

- stable case/claim IDs;
- claim text;
- supplied passages and passage IDs;
- source-boundary input metadata;
- input-side construction/phenomenon metadata;
- declared distractor roles.

The execution manifest MUST NOT contain:

- expected verdicts;
- derivations that encode the answer;
- historical CAL outcomes;
- construction-gold scores;
- expected rule IDs;
- gold labels;
- calibration results.

The manifest is generated deterministically from the canonical builder's CASES/SOURCES input declarations without calling the builder's verdict derivation.

## Frozen policy

- support threshold: 0.70
- refutation threshold: 0.70
- signal floor: existing #36/#37 replay default
- pinned production retriever/entailer unchanged
- no threshold search
- no operator repair
- no Contract C change
- no production-path mutation

## Execution order

1. verify protected semantic surfaces are unchanged from #37;
2. generate and hash the input-only 33-case manifest;
3. assert forbidden assessment keys are absent;
4. rerun the frozen 25-case sentinel through the exact predecessor apparatus;
5. require the normalized sentinel scientific-object hash to match the frozen receipt;
6. execute all 33 Cohort A inputs through real current `run_default_audit`;
7. project each run through the frozen eligibility/operator/replay machinery;
8. emit non-authoritative parallel epistemic artifacts;
9. report internal state correspondence, first divergence stage, unknowns, operator coverage, aperture failures, execution failures, polarity transitions, and variant-group behavior.

Terminal agreement is descriptive only.

## Stop conditions

Stop scientific interpretation if:

- the sentinel does not reproduce;
- protected machinery drifts;
- forbidden assessment fields enter the manifest;
- thresholds differ from 0.70/0.70;
- an execution failure is converted into epistemic abstention;
- the runner reads legacy/gold terminal labels to reconstruct missing semantic state.

Unknown remains unknown.
