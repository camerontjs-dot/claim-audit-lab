# Pre-science development notes

No decisive event-authority cohort had been executed when this note was written.

Static adversarial inspection of the first independent source completer found one avoidable aperture: a side containing a valid event followed by a second supported event verb could leave only the first construction as a syntactically valid parse and absorb the remainder into the first event's object. This mirrors a known permissive shape in RC7F-C, whose job is measurement proposal rather than authority.

The decisive candidate therefore adds a separate precondition that each side of the unique `before|after` cue contains exactly one supported event construction before invoking the first completer. The original draft is preserved in `event_authority.py`; the decisive evaluator imports `candidate.py` so the pre-execution refinement remains visible rather than rewriting the development history.

This is a pre-science apparatus refinement, not a repair after reveal.
