# Contract C Candidate A RC2 producer conformance RC1

**Classification:** Draft Research producer-conformance successor. No production CAL mutation, Contract C promotion/version assignment, release, tag, Decision production change, Contract E / Authorization, or execution authority.

## Exact authorities

CAL:
- exact frozen V1 RC1: `a902621e8baea3063dddd7f92ba975aade305464`
- engine blob: `636734fd1341d2ae721ae9697c7ac7652b89eecc`
- relation blob: `e9e9401c92ee11d25a50dbc36e80aeb4c4cd220d`

Predecessor producer record:
- CAL Draft PR #107 terminal `FALSIFIED`
- terminal record commit `d9b8d5d94b9fb37c0e8982572610c864b3633adb`
- decisive evaluation SHA-256 `63bc3d46b8762d51c9c07ca71572d68175588e216f6b24e93660cc43db9f88eb`
- established: minimal sufficient basis groups derive from existing CAL relation/composition state via unchanged `compose()` only; exact RC1 failed only because `UNSUPPORTED_SEMANTIC_FAMILY` was unrepresentable.

Apparatus successor:
- Draft PR #96
- exact RC2 implementation freeze: `3e1c44d6e264e2baa37324b66f78165b15a927f1`
- terminal qualification record: `13c34ab04da9b332f8d6db11e2d77b389172ba86`
- terminal disposition: `QUALIFIED_FOR_CAL_PRODUCER_CONFORMANCE_RESEARCH`
- RC2 validator blob: `ad5ed3ac71a8f8680188beb363c9d952c9922b15`
- RC2 schema blob: `45ebaa1e5b7342422b4d6a19f897e72de4bb148f`

## Question

Can exact frozen CAL RC1 now project its legitimate public result state into exact Candidate A RC2 without adding a new epistemic/causal judgment, including the previously falsifying `UNSUPPORTED_SEMANTIC_FAMILY` result?

## Frozen materialization rule

The successor materializer is identical in causal method to CAL #107:
- consume only exact `AuditContext`, already-produced `AuditResult` / `PassageTrace` state, and unchanged `compose()` subset replay;
- do not rerun measurement, source completion, authority evaluation, relation derivation, retrieval, learned/NLI logic, legacy v0.5 logic, or the old flat Contract C projector;
- retain inclusion-minimal subsets whose public terminal state matches the full result as `basis_groups`;
- causal role is the union of minimal basis groups; every other retained participant is residual.

The only new public-terminal mapping relative to #107 is:

`NOT_CHECKABLE / UNSUPPORTED_SEMANTIC_FAMILY -> not_checkable / UNSUPPORTED_SEMANTIC_FAMILY`

For that exact result, basis groups must be empty and all retained passages must be `non_polarized / residual`.

## Required controls

1. All current-CAL/Phase-2 controls that passed in #107 must remain exact, including independent support/refutation, minimal mixed, #106 alternative-joint, symmetric alternative-joint, four-way mixed generalization, neutral residual, no-deciding, direct-event-order support/refute/scope refusal, determinism, exact-B/policy binding and weak flat-control discrimination.
2. The exact unsupported-family CAL result must validate under exact RC2 without relabelling.
3. A weak alias mapping the unsupported result to `no_deciding_relation` must be killed by terminal semantics and object identity.
4. Exact RC1 must remain unable to represent the unsupported-family reason, preserving the predecessor discriminator.
5. No `src/**` file may change.

## Falsifiers

- any previous causal/materialization control regresses;
- unsupported-family requires a causal basis or support/refute participant;
- unsupported-family is emitted as no-deciding or another reason;
- RC2 structure validates but reconstructed semantics disagree with CAL/frozen oracle;
- materializer requires any semantic operation beyond unchanged `compose()` over already-derived traces;
- apparatus cannot distinguish the weak flat or weak reason-alias control.

## Dispositions

- `SUPPORTED_RC2_PRODUCER_CONFORMANCE`: all controls pass. This satisfies the producer gate only and leaves independent consumer conformance unresolved.
- `FALSIFIED`: a semantic producer falsifier fires with valid apparatus.
- `INCONCLUSIVE`: apparatus/evaluator/frozen authority cannot discriminate.
- `SUPERSEDED`: an exact authority changes before disposition.

No production promotion follows automatically from support.