# CAL Population/Membership Gate-1B Authority RC0 — Preregistration

Date: 2026-09-17

Classification: Draft Research / independent source-completion and warrant qualification.

Parent: terminal Population Gate-1A head `6002cb73f8dfe50dc17a40caca2a0bc412d13b3b`.
Selected measurement path: `conservative_hybrid`.

## Question

Can an independent authority layer reconstruct the exact bounded membership or subset atom from source text and refuse measurement proposals with altered entity identity, membership polarity, population identity, or subset direction?

## Frozen clean jurisdiction

The authority candidate must warrant representative qualified surfaces for:
- positive entity membership;
- negative entity membership;
- explicit `member of`;
- plural subset assertion;
- `all` and `every` universal subset forms;
- alternate entity/population bindings;
- selected Gate-1A safe extensions such as `belongs to`, `include`, explicit `subset of`, and `serves as`.

## Frozen mismatch falsifiers

Starting from a legitimately CLAIMED Gate-1A measurement, the evaluator mutates exactly one material field and requires refusal:
- membership polarity;
- entity identity;
- population identity;
- subset child;
- subset parent;
- subset direction.

## Warrant contract

A candidate may warrant only when it independently parses the source and reconstructs exactly one `MembershipAtom` or `SubsetAtom` identical to the measured atom.

Cross-passage membership inheritance is not part of this authority test. A source completer must not invent the companion subset or membership fact.

## Acceptance

`SUPPORTED_GATE1B_POPULATION_RC0` requires all clean cases warrant, all frozen mutations refuse, replay is deterministic, a weak trust-measurement authority is detected, and Gate-1A / production files remain unchanged.

Broader pressure testing is deferred until every family is finalized.
