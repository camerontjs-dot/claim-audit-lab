# RC7F-B Apparatus Notes

Pre-held-out only.

- Exact parent evidence: `34e9bcafad2c63c9b0761ffc456532344bc75b88`.
- RC7E observed `0/4` comparison dimensions with its complete proposal union.
- RC7F-B is measurement-only. It cannot authorize its own proposals.
- The first uncommitted prototype allowed a case-insensitive entity regex to greedily absorb the following lowercase verb (for example `Team A reviewed`). This was detected before qualification and before any held-out cohort. The committed candidate uses case-sensitive proper-name spans with comparison cues matched independently.
- Quantity extraction and comparison direction/attachment remain separate concerns. Numeric delta text is preserved when present rather than used as authority.
