# NEXT — after Shadow Reconciliation / Semantic-Operator RC

## Current disposition

**OBSERVED:** the 25-case predecessor scientific object reproduced exactly modulo execution-head metadata; protected semantic surfaces remained unchanged; e2e-08 and e2e-09 remained explicit rather than repaired; all successor invariants passed.

**INFERENCE:** a parallel epistemic artifact is justified as a non-authoritative research observation surface.

**UNKNOWN:** operator coverage and artifact truthfulness have not yet been demonstrated on a larger heterogeneous corpus or for an independent downstream consumer.

No production promotion follows from this RC.

---

## 1. Freeze the next larger input manifests

Before another scientific run:

1. mechanically materialize an **input-only** manifest for all 33 Construction Invariant Gold cases from the frozen source;
2. exclude expected verdicts, expected rules, historical CAL outcomes, and expected hashes from the execution aperture;
3. assign semantic-phenomenon strata before seeing successor/legacy comparisons;
4. record exact source, manifest, runner, model, threshold, and artifact-emitter receipts;
5. rerun the frozen 25-case sentinel and require the normalized predecessor scientific object to reproduce.

Do not execute the larger study until these receipts are frozen.

---

## 2. Preserve `inapplicable` separately from `unknown`

The e2e-08 experiment showed a useful distinction that exists conceptually but is not first-class in the predecessor operator-shadow schema:

- `inapplicable`: this operator has no authority over the phenomenon / cannot construct its required target;
- `unknown`: an applicable semantic assessment remains unresolved, incomplete, missing, or abstained.

### Smallest next implementation question

Can this distinction be represented **inside the research artifact/operator receipt only**, without changing production `AuditTrace`, the explicit production-candidate machinery, or Contract C?

Prefer that smaller representation test before changing shared semantic APIs.

### Falsifier

If consumers cannot make any safer or more truthful distinction from `inapplicable` versus `unknown`, or if adding the state requires reconstructing semantics from terminal legacy verdicts, do not promote the distinction.

---

## 3. Design a typed numeric/relational semantic falsifier, not a verdict patch

For e2e-08, the missing assessment is not “more A4.” The next discriminating experiment should test a narrow typed relation surface over frozen synthetic/receipt-bound cases covering:

- scalar equality and inequality;
- units and comparability;
- upper/lower bounds;
- percentages;
- compound propositions with one matching and one mismatching numeric clause;
- contradictory versus merely different quantities;
- explicit negation of a numeric proposition;
- a control like the existing quantity case where A4 already has a valid canonical complement.

### Required output

The operator should first answer questions such as:

- are the quantities referring to the same property/entity/scope?
- are units comparable?
- is the relation equality, inequality, interval inclusion, or unknown?
- which exact passage/claim spans and receipts establish the relation?

Only after those are measured should the decision model be asked whether a validated refutation exists.

### Hard boundaries

- no NLI-threshold change;
- no tuning to make e2e-08 match legacy;
- no use of expected production verdict as semantic gold;
- no assumption that numerical difference equals contradiction;
- no general operator promotion from one case.

---

## 4. Execute the preregistered larger shadow only after the input freeze

Use `CROSS_CORPUS_PREREGISTRATION.md`.

Primary analysis remains:

- first divergence stage;
- raw → eligible → semantic-valid compression;
- unknown creation/removal;
- operator applicability and terminal coverage;
- unmeasured semantic states;
- support↔adverse transitions;
- aperture failures;
- execution failures;
- exact causal/source/model/operator receipts.

Terminal agreement is descriptive only.

If a second SLG DEV corpus cannot be frozen without label leakage or output-dependent selection, report `STUDY_BLOCKED` for the cross-corpus claim rather than weakening the aperture.

---

## 5. Add an independent artifact consumer before any production discussion

The next promotion-relevant evidence should come from an **independent consumer** that reads the parallel artifact without importing CAL's legacy terminal verdict logic.

Candidate consumer tasks:

- produce a failure-localization report from raw/eligible/valid/unknown states;
- verify exact causal basis and passage provenance;
- distinguish execution failure from epistemic abstention;
- flag operator coverage holes without converting them into adverse conclusions.

### Promotion falsifier

If the consumer must reinterpret unknowns, infer missing semantic state, or depend on hidden CAL rule semantics to function, the artifact is not yet a sufficient independent contract.

Do not use Contract C as the escape hatch for missing fields. Contract C semantics are frozen and out of scope for this experiment.

---

## 6. Defer NLI model comparison until an error target exists

The current focal evidence does not isolate a model defect strongly enough to justify replacement.

A model-comparison experiment becomes appropriate only when a frozen case set has an independently specified semantic target that distinguishes:

- retrieval failure;
- NLI measurement error;
- operator applicability failure;
- semantic operator error;
- aggregation/policy error;
- missing state.

At that point preregister candidate models/calibration methods separately and keep the current pinned DeBERTa result as one frozen arm.

---

## 7. Promotion boundary

Do not propose production promotion until at least all of the following exist:

1. larger heterogeneous shadow evidence;
2. operator-coverage measurements by phenomenon;
3. preserved unknown monotonicity under mutations;
4. explicit aperture/completeness behavior outside the 25-case fixture;
5. an independent artifact consumer;
6. a separate promotion decision identifying the **smallest** production change justified by those results.

Research PR #37 remains an evidence record, not production authorization.
