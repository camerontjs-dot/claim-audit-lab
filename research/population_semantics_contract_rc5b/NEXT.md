# RC5B Next Boundary

RC5B closes the bounded representation-sufficiency question for population/membership semantics.

Do not extend the typed relation evaluator merely to add more examples of the same solved dimensions.

The next discriminating research question, if resumed after architecture review, is:

> Can a fresh text-to-typed-authority extractor recover the supported fields from held-out natural-language constructions, paraphrases, polarity reversals, entity swaps, role swaps, quantifier mutations, temporal-boundary changes, and exception forms while failing closed to explicit unknown when the authority is ambiguous or unsupported?

A successor should keep the RC5B typed authority and deterministic relation consumer frozen and put only the extraction layer under test. Prefer independent extraction implementations or an independent consumer, hidden mutations, and an explicit distinction between extraction unknown and semantic neutral.

No successor is launched from this terminal record automatically. Review the accumulated architecture evidence first.
