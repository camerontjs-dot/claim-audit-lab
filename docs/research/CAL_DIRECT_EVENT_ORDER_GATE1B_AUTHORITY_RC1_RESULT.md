# CAL Direct Event Order Gate-1B Authority RC1 — Terminal Result

Date: 2026-09-17

Classification: Draft Research / source-completion and warrant qualification.

## Frozen lineage

- M4 terminal parent: `607ec560fd53bd56279193a8d39a48b6f80e1012`
- RC0 authority failure: PR #143 / run `35294442949`
- RC1 pre-candidate apparatus head: `1b453f3e1bf55d5bf26a7f0add7edef24b157c18`
- first exposed candidate head: `e493708c38119ff11139208d8138cbd30ae62bfa`
- first exposed run: `35294832963` (semantic qualification passed; static hygiene failed)
- exact qualified RC1 head: `42fe25bd5a142277bd1903dbe65b7ea57bc94c1a`
- dedicated qualification run: `35294988006`

## Disposition

**SUPPORTED_GATE1B_DIRECT_EVENT_ORDER_RC1.**

A modifier-aware independent source-completion candidate preserves the clean direct two-event BEFORE/AFTER jurisdiction while refusing every frozen production-reachable authority leak from RC0.

## Qualified source-completion envelope

The candidate refuses:
- epistemic wrappers such as `Perhaps`;
- conditional wrappers beginning with `If`;
- negated temporal relation `not before/after`;
- fine-grained temporal modifiers such as `immediately`, `shortly`, `just`, or `directly` before/after;
- causal tails using `because` / `since`.

A warrant is issued only after:
1. measurement receipt verification;
2. exact event-order instrument identity;
3. exact one-passage consumption;
4. independent reconstruction of exactly two supported events and one unmodified BEFORE/AFTER cue;
5. exact field equality between measurement and source completion.

## Qualification evidence

Run `35294988006` passed:
- exact M4 production-parent isolation;
- frozen permissive-authority falsifier;
- candidate qualification;
- observation report;
- focused Ruff/static checks.

The clean BEFORE/AFTER and event-polarity controls all warranted.
The six production-reachable RC0 leaks all remained `CLAIMED` at measurement and were refused by RC1 authority.

## Machinery consequence

The bounded direct-event-order family now has:

```
production event-order measurement
  -> MeasurementReceipt
  -> modifier-aware independent source completion
  -> exact measurement/source equality
  -> AuthorityReceipt / warranted order atom
  -> existing direct-event-order relation algebra
```

This confirms that a separate authority layer can protect a deliberately proposal-oriented measurement instrument, provided its source parser does not repeat the same abstraction loss.

## Boundary

Research qualification only. No production `src/` mutation, no event-grammar widening, no temporal-logic expansion, no merge/release.

The end-stage pressure campaign remains deferred until all semantic families have finalized machinery.
