# CAL V1 A2 → EB → B1.2 → CAL → Parent Integration RC0 — Terminal Result

Date: 2026-09-18

Classification: Draft Research / terminal cross-repository integration evidence.

## Disposition

**SUPPORTED_CAL_V1_A2_EB_B_PARENT_INTEGRATION_RC0.**

The exact CAL research head `b695a1ca16051fde1c204987895b672e73225168` successfully executed the preregistered Contract A 2.0 → frozen Evidence Bundler V1 → released Contract B 1.2 → CAL child semantics → parent recomposition route.

## Exact subjects

CAL:
- DecompositionComposer terminal parent: `795cd7c25795a6ad08b7cca343bf7c32210cc64b`;
- exact qualified integration head: `b695a1ca16051fde1c204987895b672e73225168`;
- decisive workflow: `35368736767`.

Evidence Bundler:
- frozen integration head: `4e1f6fe00e7c350b28f52bfea14f1f8988847884`;
- profile: `eb-v1-integration-10x3-rc0`;
- config SHA-256: `sha256:5b10d0c29794e80d6876a99e26bcf6ec6a27a4c5165aee78054a6bc32759f4bc`.

Contract A:
- released `contract-a-v2.0.0`;
- promotion merge `b59c2fbe38bae78a3a35699362c0e67d17152e4b`.

Contract B:
- released `1.2.0`;
- production lock `c314e53bd91c0736aa4370a364673b069aceb43e`.

## Decisive run

Run `35368736767`: PASS.

- integrated cross-repository pressure + mutations: **13 passed**;
- frozen CAL architecture regressions: **61 passed**;
- static checks: PASS;
- exact EB checkout identity: PASS;
- CAL semantic-source freeze guard: PASS.

## Four-case result

All four preregistered cases matched exactly:

1. support + support -> parent `SUPPORTED`;
2. contradiction + support -> parent `CONTRADICTED`;
3. support + no admission -> second child `NOT_CHECKABLE`, parent `NOT_CHECKABLE`;
4. contradiction + no admission -> second child `NOT_CHECKABLE`, parent `CONTRADICTED`.

The result reproduces the qualified all_of rule under a real upstream evidence-world path rather than a synthetic child-result fixture alone.

## Boundary observations

The integrated run established:

- Contract A child identity/text/order survived into the EB native package;
- EB used the exact frozen 10/3 profile;
- admission was bound only to retained native candidates;
- non-retained candidates did not enter CAL admitted evidence;
- the explicit B1.2 compatibility carrier remained downstream of native EB identity;
- exact typed CAL targets bound exact B child claim text;
- CAL child results were hashed as immutable child-result identities;
- parent recomposition bound those exact child identities/results;
- sibling epistemic state remained isolated;
- supplied child-result call order did not become semantic authority.

## Replay

The same frozen Contract A, admission map, compatibility carrier, EB configuration, CAL targets, and CAL code replayed with:

- identical native EB package;
- identical B projection receipt;
- byte-identical C1 native CAL result;
- byte-identical C2 native CAL result;
- identical parent DecompositionResult and receipt.

## Mutation controls

The gate caught or preserved expected behavior for:

- attempted admission of a native non-retained candidate;
- child target identity swap;
- child target text-hash mutation;
- missing child result;
- extra undeclared child result;
- supplied child result reorder;
- native child result byte mutation;
- B compatibility-carrier mutation;
- Contract A child-order mutation.

Carrier mutation left native EB package identity unchanged but changed downstream carrier/receipt identity.

## Composition-layer interpretation

This route exercises:

`SemanticFamilyRegistry -> direct relation -> CAL scoreless proposition composition -> DecompositionComposer`.

It does not claim that `quantitative_change_exact_v1` is A/B-wired end to end. Quantitative composition remains separately qualified in PR #176, while scalar atomic production authority is still outside the frozen production-style family registry.

This is a preserved V1 limit, not a reason to fabricate scalar authority inside the integration test.

## Preserved deviations

Initial integration head:
- 13 integrated tests passed;
- 61 frozen CAL architecture tests passed;
- only research-test Ruff hygiene failed.

Successor head:
- scientific gates remained unchanged and green;
- Ruff check/format passed.

No CAL semantic production source changed during this integration experiment.

## Conclusion

The current CAL V1 architecture is no longer only locally compositional. It has one reconstructable cross-repository parent path through exact released/frozen upstream boundaries.

The next required gate is representation losslessness at current Contract C.

## Nonclaims

This result does not establish:

- universal Evidence Bundler recall;
- EB #119 obligation-selector production authority;
- scalar/quantitative end-to-end wiring;
- Contract C losslessness;
- Decision compatibility;
- production merge/release readiness.
