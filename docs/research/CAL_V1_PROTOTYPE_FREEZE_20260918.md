# CAL V1 Prototype Freeze — 2026-09-18

## Classification

**Research prototype freeze. Not an official SemVer release, production promotion, or tag.**

Freeze identifier:

`cal-v1-prototype-20260918`

The machine manifest is:

`research/cal_v1_prototype_freeze_20260918/FREEZE.json`

## Freeze claim

The accumulated evidence now supports freezing one exact CAL V1 **prototype architecture** for promotion design.

The freeze is intentionally narrower than “CAL solves arbitrary claims.”

It freezes the machinery that the evidence has converged on:

```text
Contract A typed/decomposed proposition authority
        ↓
Evidence Bundler / Contract B evidence world
        ↓
SemanticFamilyRegistry
  measure → warrant → direct categorical relation
        ↓
CompositionRegistry
  warranted typed atoms → bounded derived relation + receipt
        ↓
proposition-level scoreless categorical composition
        ↓
DecompositionComposer
  exact single / declared all_of parent recomposition
        ↓
native CAL child/parent result + reconstructable receipts
```

## Exact code/evidence identity

- semantic code subject: `7cf0d2e50562ec4ce4082d1e1c058a11025b1a48`;
- two-registry candidate: `7f093e954c2554c3bf21c8bd72d36550414bf54b`;
- integrated parent-path executable subject: `b695a1ca16051fde1c204987895b672e73225168`;
- pre-freeze evidence head: `647a25aed95088faf900ad2ccd8eafa0be2c91fe`.

The final freeze workflow must prove that `src/claim_audit_lab` is byte-unchanged from the qualified DecompositionComposer code subject.

## Active runtime envelope

The currently wired default family registry remains deliberately narrow:

- `strict_comparison`;
- `direct_event_order`.

The current composition registry contains:

- `quantitative_change_exact_v1`.

The current DecompositionComposer supports:

- root-only/single states;
- exact Contract A declared `all_of`.

### Important reachability limits

`quantitative_change_exact_v1` is a qualified registered composition module, but the ordinary Contract-B audit path does **not** yet produce active `scalar_value` atomic authority. Therefore quantitative change is not claimed as end-to-end A/B runtime behavior.

DecompositionComposer is qualified through the exact A2 → EB #79 → B1.2 → CAL integration harness, but the production-style CLI does not yet orchestrate a multi-child parent run as one command.

Those are promotion/integration tasks, not permission to widen semantics during this freeze.

## Research-qualified extension envelope

The Gate-0/Gate-1 evidence supports bounded contracts for:

- strict comparison;
- direct event order;
- deontic norm;
- population membership;
- scalar value;
- event occurrence;
- narrow closed attribute state;
- closed typed binary relation;
- explicit causal assertion.

Composition research further supports:

- exact quantitative change from warranted scalar points + temporal binding;
- bounded spatial composition over typed binary relations;
- occurrence/order composition with event-instance identity;
- direct-member deontic applicability with role-safe identities;
- attribute-state temporal applicability through an authority-bound scope wrapper;
- one common provenance-bearing composition receipt.

Final closure shows no additional atomic family is currently justified for:

- explicit definition/equivalence, which collapses to closed typed-binary predicates;
- explicit entity existence, which collapses to closed functional attribute state.

These contracts are extension authority, not a claim that every family has production measurement/warrant wiring today.

## Cross-repository evidence

The frozen parent integration path is:

```text
Contract A 2.0
→ Evidence Bundler #79 / eb-v1-integration-10x3-rc0
→ Contract B 1.2
→ CAL strict-comparison/direct-event-order child runs
→ immutable child results
→ DecompositionComposer
→ parent result
```

PR #178 established replay-identical native EB package, B projection receipt, CAL child result bytes, and parent receipt across support, contradiction, and no-admission combinations.

## Downstream boundary

Canonical Contract C remains `1.0.0`.

Apparatus Contracts PR #107 terminally established:

`C1_CANNOT_AUTHORITATIVELY_BIND_CAL_V1_DECOMPOSITION_RC0`.

C1 can transport opaque state strings, but cannot independently verify those strings against the exact child CAL results and DecompositionReceipt without a producer-private codec or a new typed reference surface.

Therefore this freeze stops at the native CAL result boundary.

It does not downgrade or overload C1 merely to make the diagram green.

## Preserved nonclaims

This freeze does not establish:

- universal retrieval completeness;
- source truthworthiness;
- arbitrary natural-language semantic parsing;
- all Gate-0 families as active runtime plugins;
- quantitative-change end-to-end wiring;
- inferred causality;
- rich spatial geometry/topology;
- percentage/rate/uncertainty composition;
- deep coreference;
- subset deontic inheritance or conflict resolution;
- Contract C compatibility;
- Decision Engine requalification;
- operational authorization;
- official package version or release status.

The newer Evidence Bundler obligation-selector research cycle is also outside this baseline.

## Freeze acceptance

The exact candidate may be declared frozen only if one final workflow on one exact head passes:

1. byte guard against the qualified CAL semantic code subject;
2. exact Evidence Bundler #79 checkout guard;
3. exact historical typed-binary and attribute-state authority guards;
4. integrated A2 → EB → B → CAL → parent tests;
5. final semantic closure tests;
6. composition/decomposition/plugin regressions;
7. full repository pytest under full Git history;
8. Ruff source/test checks;
9. strict mypy.

A failure is preserved and blocks the freeze until localized.

## Authorized successor

If the final freeze gate passes, the evidence supports a **minimal promotion-design step** against the real production base.

It does not authorize merge or release.

Promotion work should preserve this frozen semantics and focus on making the smallest already-qualified machinery reachable and packageable. Any new semantic capability belongs back in research.
