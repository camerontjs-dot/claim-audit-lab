# CAL Strict Comparison Gate-1B Authority RC1 — Terminal Result

Date: 2026-09-17

Classification: Draft Research / source-completion and warrant qualification.

## Frozen lineage

- M4 terminal parent: `607ec560fd53bd56279193a8d39a48b6f80e1012`
- RC0 authority failure: PR #142 / run `35294374391`
- RC1 pre-candidate apparatus head: `6a90914aab0633278727a43dc34deda3beef7366`
- exact qualified RC1 candidate head: `e085fbc0f74480a81e5b3bd4c11796a6543382e7`
- dedicated qualification run: `35294781880`

## Disposition

**SUPPORTED_GATE1B_STRICT_COMPARISON_RC1.**

A modifier-aware independent source-completion candidate preserved the current clean strict-comparison production jurisdiction while refusing every frozen production-reachable modifier leak.

## What qualified

The candidate warrants only when:
1. the production measurement receipt verifies against the exact audit context;
2. the exact strict instrument identity is present;
3. exactly one named admitted passage was consumed;
4. independent source completion reconstructs exactly one supported unmodified comparison;
5. source fields exactly equal measurement fields.

The source-completion envelope refuses:
- relation negation;
- `no higher/lower/etc.` scope;
- epistemic modality such as `probably`, `may`, `might`, `could`;
- attribution/reporting surfaces.

## Qualification evidence

Run `35294781880` passed:
- exact production-parent isolation;
- frozen permissive-authority falsifier;
- candidate qualification;
- full case-observation report;
- focused Ruff/static checks.

The frozen clean cases all warranted.
The seven production-reachable RC0 modifier leaks all remained `CLAIMED` at measurement and were then refused by RC1 authority.

## Machinery consequence

The bounded strict-comparison family now has a qualified research path:

```
production strict measurement
  -> MeasurementReceipt
  -> modifier-aware independent source completion
  -> exact measurement/source equality
  -> AuthorityReceipt / warranted comparison atom
  -> existing strict-comparison relation algebra
```

This supports the architecture separation: measurement may be a proposal instrument, but the source-completion path must not repeat the same lossy abstraction.

## Boundary

This is still research qualification. Production `src/` was not modified. RC1 does not widen comparison measurement, add equality/multiplier support to the production seam, or authorize merge/release.

The broader end-stage pressure test remains deferred until all semantic families have finalized machinery.
