# CAL Epistemic Methodology RC0A — terminal contamination record

## Disposition

**FALSIFIED**, specifically for this execution's corrected fresh-context / pre-freeze isolation claim.

This is **not** a falsification of the RC0A evaluator methodology, any candidate architecture, current CAL semantics, or a staged pipeline. The scientific architecture-discrimination experiment did not reach evaluator freeze or candidate exposure under the intended protocol.

## OBSERVED

### Live identities established before stop

- CAL production `main`: `e90f301cf6ca02c0c77b6e88c3b08f8b93b9a36a`.
- Immutable CAL `v0.5.0`: annotated tag object `193c07178a8d891231746fca97c8c1bef0e9dc3c` resolving to commit `5533bbcf27a3ee3a7d901f7dfc44c241bc558e2c`.
- Immutable Contract C `contract-c-v1.0.0`: annotated tag object `6bd135a948e407212b2e77ec18ac5c402f93565e` in `camerontjs-dot/apparatus-contracts`, resolving to commit `5fe55f9ed5d0ee9f026ca1b077e9d70ce0487ea1`.
- RC0 PR #28 live head: `cdf861b6c9974d90e844597fc84eb3e6ab2e36ea`; PR is closed, unmerged, Draft, terminally `INCONCLUSIVE`.
- RC0A branch was created from the live production baseline only after the contamination was detected, solely to preserve this terminal evidence record.

### Pre-freeze isolation breach

Before the corrected RC0A evaluator was frozen, the execution fetched the general GitHub Pull Request resource for PR #28 as part of predecessor-state verification.

That durable PR body contained information that RC0A explicitly classified as forbidden before freeze, including:

- implementation-level descriptions of historical-v2 mechanisms (explicit-removal / per-role participation behavior and early-return behavior); and
- names/descriptions of the exploratory RC0 candidate families (additive epistemic receipt and staged-ledger shadow candidates).

The RC0A protocol allowed selected durable predecessor records and direct production-path observations, but it did **not** authorize exposure to old-v2 implementation details or candidate-direction information before evaluator freeze.

The breach therefore occurred through an over-broad durable-record aperture, not through candidate code execution.

## INFERENCE

The intended RC0A clean-room claim cannot be made for this execution. Continuing to construct or freeze the evaluator after this exposure would create an avoidable possibility that fixture selection, invariants, falsifiers, or evaluator structure were influenced by forbidden candidate information.

The correct scientific action is to stop before evaluator construction/freeze and preserve the deviation.

## FALSIFIED ALTERNATIVES

- **"Durable GitHub record" is automatically safe pre-freeze.** Falsified for this protocol. A durable PR body can aggregate both allowed predecessor findings and forbidden candidate/implementation information.
- **PR #28 can be fetched wholesale before freeze.** Falsified for RC0A isolation. The aperture must be narrower than the PR resource itself.

## HYPOTHESES STILL LIVE

All architecture hypotheses remain live because none were evaluated in RC0A:

- current architecture sufficient;
- bounded additive typed state/receipt;
- partial staged decomposition;
- fuller staged pipeline;
- another smaller/differently factored architecture;
- no justified architectural change.

## UNKNOWNS

Because the experiment stopped before evaluator freeze:

- no corrected fixtures were frozen;
- no weak controls were frozen or executed;
- no evidence-presence ladder was executed;
- no trust mutation or assessment-state ladder was executed;
- no temporal/applicability/authority controls were executed;
- no causal-removal replay was executed;
- no strong policy counterfactual was executed;
- no distributed-evidence aggregation control was executed;
- no execution/failure control was executed;
- no candidate implementation was inspected as part of RC0A after the breach;
- no Contract-C compatibility conclusion was attempted beyond identity verification.

The historical-v2 head and merge-base/divergence identities were intentionally **not** reverified after the stop because the stopping rule had already fired and further scientific inspection was unnecessary.

## ARCHITECTURE DISPOSITION

**FALSIFIED** only for this execution's fresh-context isolation claim.

No architecture disposition is scientifically supported.

## CONTRACT-C COMPATIBILITY

Not evaluated. Contract C `1.0.0` identity was verified only:

`5fe55f9ed5d0ee9f026ca1b077e9d70ce0487ea1`.

No contract changes were made or authorized.

## NON-CLAIMS

This record does not claim that:

- current CAL is epistemically sufficient or insufficient;
- an additive receipt is sufficient or insufficient;
- staged decomposition is necessary or unnecessary;
- the historical v2 branch is correct or incorrect;
- Contract C requires revision;
- any production semantic, threshold, model, retrieval, Evidence Bundler, or Decision Engine change is justified.

## NEXT SMALLEST TEST

Run a new successor in another genuinely fresh context with a mechanically constrained **pre-freeze durable-record aperture**.

Before evaluator freeze, do not fetch PR #28 as a whole. Permit only exact allowlisted immutable resources, for example:

1. production `main`, immutable release/tag identities, and released production source/tests;
2. issue #3;
3. exact RC0 predecessor files whose contents are explicitly allowlisted by the successor protocol;
4. a sanitized predecessor identity manifest containing RC0 PR number/head/disposition and direct production observations only.

The source reader should reject or avoid:

- PR #28 body/comments/diff/files wholesale;
- RC0 candidate paths;
- historical-v2 paths;
- candidate-comparison tests/results/gate vectors;
- any external epistemic-abstention synthesis until after freeze.

A fresh successor should freeze its evaluator before widening that aperture.

## Deviations / execution receipts

- Contamination detected before any RC0A evaluator or fixtures were authored.
- No evaluator freeze commit exists.
- No candidate identities exist for RC0A.
- No RC0A scientific test commands or CI runs exist.
- Production `main` was not modified.
- No release/tag was modified.
- No Contract B/C, Evidence Bundler, or Decision Engine artifact was modified.

## Changed files

- `docs/research/results-04-rc0a-terminal-contamination.md` only.
