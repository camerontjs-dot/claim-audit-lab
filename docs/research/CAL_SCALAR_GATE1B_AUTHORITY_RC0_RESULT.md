# CAL Scalar Gate-1B Authority RC0 — Terminal Result

Date: 2026-09-17

Classification: Draft Research / independent source-completion and warrant qualification.

## Frozen lineage

- terminal Gate-1A parent: `4f2c6718a1cea5b0c92d00b10266794135be3c77`
- pre-candidate apparatus head: `7abb99a38e8baa16a03260554a2585ae708377ba`
- first exposed candidate head: `ec061815a3030e9efe6cc8d37280ad35b4233775`
- semantic-green/static-red run: `35295859117`
- exact qualified candidate head: `c33b656819e91ac38a85c48921b84ea20ec090bc`
- dedicated qualification run: `35297087952`

## Disposition

**SUPPORTED_GATE1B_SCALAR_RC0.**

The independent target-aware source completer reconstructs the bounded scalar atom and warrants only exact equality with the Gate-1A measurement.

## Qualified state

Authority preserves:
- entity;
- metric;
- unit;
- exact rational low/high bounds;
- exactness flag.

Qualified clean surfaces include exact integer/decimal percentages, closed ranges, counts, Celsius/Fahrenheit values, approximate values, terse field notation, and plus/minus uncertainty represented as a non-exact interval.

## Falsifiers

The frozen evaluator mutates entity, metric, unit, point value, interval bounds, and exactness. Every mutation is refused. A weak trust-measurement authority is rejected.

No unit conversion is performed.

## Machinery consequence

```
conservative scalar measurement
  -> ScalarAtom proposal
  -> independent target-aware source reconstruction
  -> exact atom equality
  -> scalar warrant
  -> Gate-0 scalar relation algebra
```

Exactness is semantic state and is not discarded during warrant.

## Preserved deviation

The first candidate run was semantically green but Ruff rejected frozen-evaluator import layout. The terminal branch uses a file-scoped `I001` suppression without changing evaluator semantics.

## Boundary

Research qualification only. No unit conversion, tolerance policy, significant-figure policy, production wiring, merge, or release. Broad pressure testing remains deferred until every family is finalized.
